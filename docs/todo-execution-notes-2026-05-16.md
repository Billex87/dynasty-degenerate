# Todo Execution Notes - 2026-05-16

Scope: one practical pass through the remaining `todo.md` items after the AI-read and rookie-draft fixes. UI items received a single cleanup pass only so the larger premium/Three.js work can stay on the roadmap instead of consuming the whole session.

## Completed In This Pass

| Area                               | Result                                                                                                                                                                                                                      | Files                                                                                |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Autopilot stale labels             | Removed visible placeholder/product-internal copy from the AI Autopilot shell. Empty status now falls back to `Live`, schedule context is labeled as matchup planning, and the old phase badge is no longer shown to users. | `client/src/components/AITeamAutopilot.tsx`                                          |
| Autopilot access feedback          | Added a one-time toast when a non-admin path tries to open the admin-only Autopilot tab, then returns the user to Overview.                                                                                                 | `client/src/pages/Home.tsx`                                                          |
| Overview/dashboard copy polish     | Renamed the mobile Overview tab label from `View` to `Home`, replaced the generic Monthly Team Blueprint kicker, and changed the History preview tone so weekly risers read as informational instead of warning-style.      | `client/src/pages/Home.tsx`                                                          |
| Admin surface naming               | Replaced several `Admin Eyes Only` section titles with specific operational labels so the admin sections read as product surfaces instead of internal placeholders.                                                         | `client/src/pages/Home.tsx`                                                          |
| Mobile metric tile width           | Raised the mobile modal metric-grid minimum from `4rem` to `5.5rem` in the responsive/foundation styles so compact owner and starter metric tiles are less likely to become unreadable on narrow screens.                   | `client/src/styles/report-responsive.css`, `client/src/styles/report-foundation.css` |
| Legacy action-plan storage cleanup | Removed the old `trade-recommendation-outcomes:v1` localStorage migration bridge now that shared action-plan storage has been live. The current action-plan key remains the only read/write path.                           | `client/src/components/CommandCenterExpansion.tsx`, `todo.md`                        |
| GitHub Actions runtime upgrade     | Upgraded CI and Production Smoke workflows to the current `actions/checkout` and `actions/setup-node` major versions, then fixed the optional snapshot replay regression so CI skips gitignored local fixtures when absent.   | `.github/workflows/ci.yml`, `.github/workflows/production-smoke.yml`, `server/snapshotReplayRegression.test.ts` |
| Admin visibility E2E coverage      | Added a focused command-center regression that proves admin-only tabs, diagnostics, and trade reads appear only in admin feature mode while removed hidden Sleeper import controls stay absent.                              | `tests/e2e/report-command-center.spec.ts`, `todo.md`                                 |
| Command action cleanup             | Removed clipboard-copy buttons, Sleeper open shortcuts, waiver submit language, and the hidden Sleeper token-import panel from the report UI so recommendations stay as internal save/watch decisions.                      | `client/src/components/ReportTables.tsx`, `client/src/components/reportTables/WaiverIntelligencePanel.tsx`, `client/src/components/CommandCenterExpansion.tsx`, `client/src/pages/Home.tsx` |
| Persistence E2E coverage           | Added Playwright regressions for local watch preference persistence, portfolio snapshot persistence, and print-focused blueprint export controls after removing clipboard-copy actions.                                  | `tests/e2e/report-command-center.spec.ts`, `todo.md`                                 |
| Weekly movement relevance gate     | Added a server-side weekly momentum eligibility check so fringe/unranked values do not become user-facing riser/faller rows. Cache versions were bumped so old report payloads do not keep stale movement rows visible. | `server/reportGenerator.ts`, `server/reportGenerator.test.ts`, `server/routers.ts`, `client/src/pages/Home.tsx`, `tests/e2e/fixtures/cachedReports.ts`, `todo.md` |
| Alternate player news source       | Added a snapshot-backed SportsDataIO/RotoBaller news adapter and shared player-news aggregator so reports and player modals can merge FantasyPros plus licensed SportsDataIO news without live provider calls during report loads. | `server/sportsDataNews.ts`, `server/playerNews.ts`, `server/reportStaticInputs.ts`, `server/dynamicDataJobs.ts`, `server/routers.ts`, `server/sourceSnapshotFreshness.ts`, `server/sourceCoverageMatrix.ts`, `todo.md` |
| Player AI explanation traces       | Attached cohort explanation traces to player detail AI reads and added draft-capital runway scoring so first-round, day-two, late-round, and low-capital profiles get different opportunity patience. | `server/playerCohortEngine.ts`, `server/routers.ts`, `shared/types.ts`, `client/src/components/PlayerDetailModal.tsx`, `client/src/styles/report-responsive.css`, `todo.md` |
| Redraft draft-history semantics    | Preserved prior-year redraft draft history when draft data exists, while adding cached no-draft coverage so empty redraft draft surfaces stay hidden and `#draft` falls back to Overview. | `client/src/pages/Home.tsx`, `tests/e2e/fixtures/cachedReports.ts`, `tests/e2e/report-state-redraft.spec.ts`, `tests/e2e/live-admin-report.smoke.spec.ts`, `todo.md` |
| Mobile AI readout compaction       | Full AI readouts now collapse on mobile into short cards with title, confidence, one-line takeaway, and `Read more`, with Playwright coverage across Overview, Blueprint, Rankings, and Trade History. | `client/src/components/AIReadPanel.tsx`, `client/src/styles/report-responsive.css`, `client/src/styles/ai-tron-surface.css`, `tests/e2e/report-command-center.spec.ts`, `todo.md` |
| Overview ownership enforcement     | Tightened the rendered Overview stack so Power Rankings stays ranking-only, Team Breakdown removes trade-chip/sell-candidate ownership leaks, and Trade Finder remains the owner for specific packages and targets. | `client/src/components/CommandCenterExpansion.tsx`, `tests/e2e/report-command-center.spec.ts`, `todo.md` |
| AI readout coverage diagnostics    | Added an admin-only diagnostics section that counts readouts by tab, flags missing confidence/traces, duplicate-risk, and source-limited reads, and keeps the panel hidden from regular report viewers. | `client/src/pages/Home.tsx`, `client/src/styles/report-foundation.css`, `tests/e2e/report-command-center.spec.ts`, `docs/ai-readout-feature-coverage.md` |
| Visible readout traces             | Promoted `Why this fired` traces into the shared AI read panel and wired them into owner-level power, roster, trade, and Owner Intel reads so users can see the signals behind the recommendation. | `client/src/components/AIReadPanel.tsx`, `client/src/components/CommandCenterExpansion.tsx`, `client/src/components/ReportTables.tsx`, `tests/e2e/report-command-center.spec.ts`, `docs/ai-readout-feature-coverage.md` |
| Report-generated 3D success card   | Upgraded the live loading-dialog success card into the saved-for-last 3D moment: football fly-by, field-depth grid, medallion core, stamp shockwaves, production fallback, and desktop/mobile canvas checks. | `client/src/components/SuccessCard3D.tsx`, `client/src/styles/dd-success-techpanel.css`, `tests/e2e/report-command-center.spec.ts`, `todo.md` |
| Owner Intel PCB readout grid       | Wrapped the Owner Intel AI reads in a connected PCB system with shared cyan/orange route buses, glowing nodes, stronger board rails, per-card AI routes, and a full-width Dynasty AI Notes bottom rail. | `client/src/components/ReportTables.tsx`, `client/src/styles/ai-tron-surface.css`, `tests/e2e/report-command-center.spec.ts`, `todo.md` |
| nflverse draft-capital source      | Added a snapshot-backed nflverse/ffverse draft-capital feed from public player ID data, wired it into dynamic refresh, admin freshness/coverage diagnostics, and player cohort runway enrichment without live report-load calls. | `server/nflverseDraftCapital.ts`, `server/dynamicDataJobs.ts`, `server/routers.ts`, `server/sourceSnapshotFreshness.ts`, `server/sourceCoverageMatrix.ts`, `todo.md` |
| nflverse role/investment context   | Added compact nflverse usage, snap, injury, combine, and contract snapshots; report loads read stored summaries and player cohort traces now include usage trend, injury history, athletic profile, veteran contract runway, and news/value movement notes. | `server/nflversePlayerContext.ts`, `server/playerCohortEngine.ts`, `server/reportPlayerEnrichment.ts`, `server/routers.ts`, `server/sourceSnapshotFreshness.ts`, `server/sourceCoverageMatrix.ts`, `shared/types.ts`, `todo.md` |
| Player read calibration            | Added cohort evidence calibration so player AI reads expose an evidence grade, strong-read eligibility, confidence cap, missing signals, caution flags, and UI chip before the app presents a high-confidence take. | `server/playerCohortEngine.ts`, `server/playerCohortEngine.test.ts`, `shared/types.ts`, `client/src/components/PlayerDetailModal.tsx`, `todo.md` |
| Player Situation Delta source plan | Audited public nflverse and local prospect coverage, documented the all-player situation-delta model, separated public-data fallbacks from licensed route/YPRR gaps, and added a reusable source audit script for future refreshes. | `docs/player-situation-delta-data-plan.md`, `scripts/audit-situation-delta-sources.mjs`, `docs/source-onboarding-and-coverage-audit.md`, `package.json`, `todo.md` |
| Player situation usage/team context | Migrated the nflverse usage snapshot to season-specific `stats_player` rows with target share, air-yard share, and WOPR, added `stats_team` team-environment snapshots, and exposed team pass/run context in player cohort traces. | `server/nflversePlayerContext.ts`, `server/nflversePlayerContext.test.ts`, `server/playerCohortEngine.ts`, `server/sourceSnapshotFreshness.ts`, `server/sourceCoverageMatrix.ts`, `shared/types.ts`, `todo.md` |
| Player situation PBP splits         | Enriched team-environment snapshots with nflverse play-by-play splits for neutral-script pass rate, red-zone pass/rush tendency, non-garbage pass rate, no-huddle rate, and estimated seconds per offensive play. | `server/nflversePlayerContext.ts`, `server/nflversePlayerContext.test.ts`, `server/sourceCoverageMatrix.ts`, `shared/types.ts`, `docs/player-situation-delta-data-plan.md`, `todo.md` |
| Player roster-room deltas           | Added first-pass roster-room snapshots from nflverse rosters and depth charts, comparing current roster season against the previous season for same-position additions, losses, rookie/premium additions, crowding, and vacated-opportunity signals. | `server/nflversePlayerContext.ts`, `server/nflversePlayerContext.test.ts`, `server/playerCohortEngine.ts`, `server/sourceSnapshotFreshness.ts`, `server/sourceCoverageMatrix.ts`, `shared/types.ts`, `docs/player-situation-delta-data-plan.md`, `todo.md` |
| Player roster movement typing       | Added weekly roster timing and nflverse trades overlays to roster-room snapshots so additions/losses carry movement type, confidence, first/last seen week, status timing, and trade team/date context when public data supports it. | `server/nflversePlayerContext.ts`, `server/nflversePlayerContext.test.ts`, `server/playerCohortEngine.ts`, `server/sourceCoverageMatrix.ts`, `shared/types.ts`, `docs/player-situation-delta-data-plan.md`, `docs/source-onboarding-and-coverage-audit.md`, `todo.md` |
| Player roster movement quality      | Joined roster-room additions/losses to prior-season usage so movement now carries target/carry/reception/PPR context, quality tier, impact score, net opportunity score, and top-returning-player promotion signal for JSN-style breakout reads. | `server/nflversePlayerContext.ts`, `server/nflversePlayerContext.test.ts`, `server/playerCohortEngine.ts`, `server/playerCohortEngine.test.ts`, `server/sourceCoverageMatrix.ts`, `shared/types.ts`, `docs/player-situation-delta-data-plan.md`, `docs/source-onboarding-and-coverage-audit.md`, `todo.md` |

## Kept On The Roadmap

| Item                               | Why It Was Not Claimed Done                                                                                                                                                                                  |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Report shell Three.js backdrop     | The generated-report modal and Owner Intel PCB pass are complete; the broader report shell backdrop is paused by request so non-UI/data work can continue first.            |
| Billing/auth/Stripe                | The product plan exists, but implementing it safely needs provider choices, Stripe product IDs, webhook secrets, legal pages, entitlement rules, and abuse controls.                                         |
| Props production rollout           | The code path is snapshot-first, but real OpticOdds/Sleeper/Underdog/bet365-style betting signals still require approved keys, real snapshots, coverage audits, and threshold tuning.                        |
| FantasyPros remaining endpoints    | Rookie/ADP/projection/injury/news/player-points work should stay blocked until endpoint terms, rate limits, and snapshot behavior are verified for production use.                                           |
| Account-level persistence          | Watchlists, saved leagues, player shares, portfolio exposure, and saved report artifacts need account/auth infrastructure before they should replace local browser storage.                                  |
| Exports and share links            | PDF/image/shareable report artifacts need a separate rendering and storage pass so exports look intentional and do not leak private league data.                                                             |
| ReportTables module split          | The current split stubs still depend on helpers and state in `ReportTables.tsx`. A safe split should extract shared helpers first, then move one table module at a time with bundle verification.            |
| E2E coverage expansion             | Command Center persistence, admin visibility, mobile AI readout compaction, and Overview ownership now have focused Playwright coverage; deeper matchup/projection QA still waits for stable real data.       |

## Next Best Target

UI upgrades are paused. The highest-value next data target is turning the stored nflverse context into richer comps and waiver/trade scoring: role-growth alerts, contract-cliff warnings, and draft-capital-plus-usage historical comparison buckets.
